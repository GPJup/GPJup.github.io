const canvas=document.getElementById('particles'),ctx=canvas.getContext('2d');let w,h,dpr,particles=[],mouse={x:-999,y:-999,active:false,color:'#8b5cf6'};let palette=['#8b5cf6','#a78bfa','#6366f1'];const cards=[...document.querySelectorAll('.card')];function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);particles=Array.from({length:Math.min(240,Math.max(90,Math.floor(w*h/7000)))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.2,vy:(Math.random()-.5)*.2,r:.5+Math.random()*1.5,c:palette[Math.floor(Math.random()*palette.length)],life:Math.random()*6.28}))}function rgb(hex){hex=hex.replace('#','');if(hex.length===3)hex=hex.split('').map(x=>x+x).join('');let n=parseInt(hex,16);return[n>>16&255,n>>8&255,n&255]}function mix(a,b,t){let A=rgb(a),B=rgb(b);return`rgb(${A.map((v,i)=>Math.round(v+(B[i]-v)*t)).join(',')})`}function frame(){ctx.clearRect(0,0,w,h);for(const p of particles){p.life+=.008;p.x+=p.vx+Math.sin(p.life)*.025;p.y+=p.vy+Math.cos(p.life)*.025;if(p.x<-20)p.x=w+20;if(p.x>w+20)p.x=-20;if(p.y<-20)p.y=h+20;if(p.y>h+20)p.y=-20;let dx=p.x-mouse.x,dy=p.y-mouse.y,dist=Math.hypot(dx,dy),a=.2,c=p.c;if(mouse.active&&dist<155){let t=1-dist/155;c=mix(p.c,mouse.color,t);a=.2+.7*t;if(dist>0){p.x+=dx/dist*t*.12;p.y+=dy/dist*t*.12}}ctx.globalAlpha=a;ctx.fillStyle=c;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;requestAnimationFrame(frame)}addEventListener('pointermove',e=>{mouse.x=e.clientX;mouse.y=e.clientY;mouse.active=true});addEventListener('pointerleave',()=>mouse.active=false);cards.forEach(card=>{let c=card.dataset.color;card.style.setProperty('--link-color',c);card.addEventListener('pointerenter',()=>{mouse.color=c;document.documentElement.style.setProperty('--accent',c)});card.addEventListener('pointermove',e=>{let r=card.getBoundingClientRect();card.style.setProperty('--mx',`${e.clientX-r.left}px`)})});resize();addEventListener('resize',resize);frame();
